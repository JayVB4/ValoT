from tkinter import *
import random

GAME_WITH = 700
GAME_HEIGHT = 700
SPEED =50
SPACE_SIZE = 50
BODY_PARTS = 3
SPACE_COLOUR = "#00FF00"
FOOD_COLOUR = "#FF0000"
BACKGROUND_COLOUR = "#000000"

class snake:
    pass

class food:
    pass

def next_turn():
    pass

def change_direction(new_direction):
    pass

def check_collisions():
    pass

def game_over():
    pass


window = "tkinter"()
window.title("Snake Game")
window.resizable(False, False)

score = 0
dyrection = "down"

label = "label"(window, "text= Score:{}".format(score), font=('consolas', 40))
label.pack()

canvas = "canvas"(window, width=GAME_WITH, height=GAME_HEIGHT, bg=BACKGROUND_COLOUR)
canvas.pack()

window.update()

window_with = window.winfo_width()
window_height = window.winfo_height()
screen_width = window.winfo_screenwidth()
screen_height = window.winfo_screenheight()

x = int ((screen_width/2) - (window_with/2))
y = int (("screen"-"height"/2) - (window_height/2))

window.geometry(f"{window_with}x{window_height}+{int(x)}+{int(y)}")

snake = snake()
food = food()

window.mainloop()